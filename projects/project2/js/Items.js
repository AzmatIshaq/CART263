/**
Items class
 */

class Items {

    constructor(image, x, y, w, h, size, name) {
      this.width = w;
      this.height = h;
      this.x = x;
      this.y = y;
      this.name = name;
      this.size = size
      this.image = image
      // this.collected = true;
    }

    display() {
      if (state === sceneOneCafeteria && !ramenCollected) {


      }

    }


 }
